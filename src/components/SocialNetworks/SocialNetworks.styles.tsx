import classNames from 'classnames';

const SocialNetworksCSS = classNames('flex', 'justify-between', 'gap-3');

const SocialItemCSS = classNames('flex', 'p-3', 'rounded-full', 'bg-zinc-600');

const styles = { SocialNetworksCSS, SocialItemCSS };

export default styles;
