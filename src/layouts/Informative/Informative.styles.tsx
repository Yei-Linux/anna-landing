import classNames from 'classnames';

const ActionSpanCSS = classNames('flex', 'items-center', 'gap-2');

const InformativeCSS = classNames('flex', 'flex-col', 'gap-y-10');

const styles = { ActionSpanCSS, InformativeCSS };

export default styles;
